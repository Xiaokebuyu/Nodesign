# SESSION 2026-04-30 — P0 改动总览

> 起点：reset 回 `deec72d`（19:07 e2e ✅，hello-world LLM 跑通），整晚 yolo 8 commit
> 移到 `yolo-night-2026-04-29` 分支保留。
> 本次：拆 10 个 commit 把"P0 主线 5 流"从地基重做，每段停下让用户 review。

## TL;DR

| 维度 | 数 |
|---|---|
| 新增 commit | **9 个**（C1-C9，C10 即本文档） |
| 改动文件 | 后端：9 新 + 3 改；前端：3 新 + 7 改 |
| 累计 +/- | +2461 / -372（含 hotfix） |
| 验证粒度 | 每个 commit syntax check / smoke / vite build；最后跑过一次真 LLM e2e |

## P0 范围（5 流 × 主线）

| 流 | P0 交付 | 实现 commit |
|---|---|---|
| **A** 自由创作 | brief → POST /turn → agent 写 canvas.html → iframe 显示 | C1 (workspace) + C2 (skill+server) + C3 (turn endpoint) + C5 (lib) + C6 (Project.jsx 真接) |
| **B** 引用上下文 | 附件托盘 → 上传 → POST /turn 带 attachments → agent 自己 Read | C3 (assets endpoint) + C8 (托盘 + 真接) |
| **C** chat 全局重规划 | 复用 POST /turn，prompt 自由文本 | C6（同 A 路径） |
| **E** direct edit | iframe 双击改字 → blur → outerHTML → PUT /canvas + git commit | C3 (canvas PUT) + C7 (Project.jsx 真接) |
| **I** 导出 | 顶栏 ExportMenu → GET /exports/{html\|pdf\|handoff} | C4 (3 endpoint) + C9 (前端按钮真接) |

D / F / H 三流（inline comment / custom slider / DS 抽取）**不在 P0 范围**，留 P0+
继续头脑风暴细节后再做。

## 10 个 commit（按时间序）

```
9e27924  fix: vite proxy 加 /ws 路径 — agent 事件流终于能流到前端 ★ 关键 hotfix
6178437  P0 C9: 流 I 导出 — 用户主动按钮（HTML / PDF / Handoff）
853cc79  P0 C8: 流 B 引用上下文 — 附件托盘 + 上传走 turn
16722da  P0 C7: 流 E direct edit 真接 — outerHTML PUT /canvas + git commit
0026aca  P0 C6: 流 A/C 跑通 — Project.jsx + Home + CreateProjectModal 真接
d5fc205  P0 C5: 前端基础设施层 — api.js / ws-client.js / projectStore 真接
e1d5c25  P0 C4: 用户主动导出 endpoints (HTML / PDF / Handoff)
822bfb9  P0 C3: 业务 endpoints 全套 — projects / canvas / skills / assets / turn
ec68120  P0 C2: Express 4001 入口 + WS broker + 默认 skill
f489a3d  P0 C1: 后端 scaffold — projects workspace 模型 + agent loop 接 workspaceRoot
```

### ★ 重要 hotfix（9e27924）— vite proxy /ws 路径

**症状**：用户点 chat send 后看不到任何 agent 流式回复。

**误诊路径**：
- POST /api/projects/:pid/turn 端点 → curl 测 202 + runId ✓
- 后端 runAgent 异步启动 → 真触发 LLM ✓
- 事件 publish 到 project EventBus ✓

但前端永远收不到事件流。

**根因**：`web/vite.config.js` proxy 配置只覆盖了 `/api`：

```js
// 之前（漏了）
proxy: {
  '/api': { target: 'http://localhost:4001', ws: true },
}
```

`ws: true` 在 `/api` 下只覆盖 `/api/*` 的 WebSocket 升级（HMR 之类）。
WS 升级路径 `/ws/projects/:pid` 不在 `/api` 下 → vite 不转发 → 前端连
`ws://localhost:5174/ws/projects/:pid` 立即被 vite 关掉 → 前端永远没收到事件。

**修复**：vite.config 加单独的 `/ws` proxy entry，`target: ws://localhost:4001`。

```js
proxy: {
  '/api': { target: 'http://localhost:4001', changeOrigin: true },
  '/ws':  { target: 'ws://localhost:4001', ws: true, changeOrigin: true },
}
```

**验证（preview chrome 在新 vite 上跑真 e2e）**：

```
ws.connected → _sent (202) → run.start → run.sdk.session →
run.delta.text (22 chars) → run.delta.tool_use (Read) →
run.delta.tool_result → ... 17 events 流过来 ★
```

agent 真在跑 hello-world skill，多次 Read 工具调用，事件流式推到前端。

**教训**：写 plan / 写 commit message 自信"vite proxy ws: true 转发了"——
没真测过 WS 端到端。下次 Backend + Frontend 联调，必须真发一条 turn 看
前端能否收到事件，不能只靠 endpoint 200 / 编译通过。

每个 commit 都有详细改动说明（git show <hash> 看），范围严格 ≤ 5 文件。

## 架构骨架（已锁，不要再改）

NoDesign **不是**独立 agent 系统，是 **Claude Code 之上的画布编辑层**：

- 底层（session 管理 / 工具集 / skill 加载 / file checkpoint）直接用 Claude Agent SDK
- 我们做：薄壳后端（包 SDK + REST + WS 转发）+ 前端"画布编辑层"产品价值

```
┌─ Claude Agent SDK (我们用) ──────────────────┐
│  ✓ session 管理     ✓ Read/Edit/Write/Bash  │
│  ✓ Task (sub-agent) ✓ SKILL.md / plugin     │
│  ✓ file checkpoint                          │
└──────────────────────────────────────────────┘
                ↑ 我们直接用
┌─ NoDesign 薄壳 ──────────────────────────────┐
│  /api/turn       包 query() + WS 流转发      │
│  /api/canvas     PUT 用户直改 / GET 当前 HTML │
│  /api/exports    HTML / PDF / Handoff zip   │
│  /api/assets     multipart 上传              │
│  /api/projects   CRUD + workspace 管理       │
│                                                │
│  workspace 布局：                            │
│  server/projects-data/<pid>/                 │
│    ├── canvas.html     ← 主产物              │
│    ├── spec.json       ← agent 私域记忆      │
│    ├── assets/                               │
│    ├── .claude/skills/                       │
│    └── .git/           ← history             │
└────────────────────────────────────────────────┘
                ↓
┌─ 前端 React (产品价值)─────────────────────┐
│  iframe canvas + chat composer + 附件托盘  │
│  direct edit / export menu                 │
└────────────────────────────────────────────┘
```

## 5 条产品/架构原则（写进 deskskill-engine-mini SKILL.md）

1. **Agent 解析 → 思考 → 对齐 → 动手不揣测**，模糊一律提问
2. **多 modality 信号 = chat 消息附件**：不各自触发 agent，用户 send 时一起送
3. **意图明确则收敛，模糊则建议变体**（agent 内嵌策略）
4. **export 双入口**：用户按钮 + agent 工具（P0 只做按钮，agent 工具 P0+）
5. **Agent 写完 HTML 倾向截图自检**（P0+ 启用，需 vision input）

## 已 e2e 验证的（curl + preview MCP）

后端（C3 / C4）：
- ✅ POST /api/projects → 201 + workspace mkdir + git init + spec.json 占位
- ✅ GET /api/projects → 列项目
- ✅ GET /:pid/canvas（不存在）→ 200 placeholder html
- ✅ PUT /:pid/canvas → 写文件 + git commit + 返 sha
- ✅ GET /:pid/canvas/history → git log entries
- ✅ POST /:pid/assets multipart → 写 ./assets/
- ✅ GET /api/skills → global 含 deskskill-engine-mini + hello-world
- ✅ DELETE /:pid → 204 + workspace 目录清理
- ✅ GET /:pid/exports/html → 200 / text/html
- ✅ GET /:pid/exports/handoff → 200 / zip with 6 entries
- ✅ GET /:pid/exports/pdf → 200 / "PDF document, version 1.4, 1 pages"

前端（preview MCP screenshot + console 检查）：
- ✅ Home mount + hydrate：4 个 stale yolo project 拉到，DeskSkill 风格完整
- ✅ Project mount + hydrate：iframe placeholder（"canvas.html 还没生成"）
- ✅ /api/projects 200（vite proxy → :4001 工作）
- ✅ console 无 error
- ✅ WS 连接成功（无 error）
- ✅ DELETE 项目从 Home 工作（toast "项目已删除" 出现）

每个 C5-C9 commit 都跑过 `vite build`，1.5-1.9s 全通过。

## 未在本次 commit e2e 验证（留给后续浏览器手动测）

hotfix 后 hello-world skill 一次真跑通过；剩下的留给用户在浏览器手动测：

1. **deskskill-engine-mini 真跑（A 流主路径）** — 默认 skill 用真 brief 跑
   一次（~30s ~$0.1），看 agent 写 canvas.html + 流式翻译完整
2. **direct edit 真 PUT 落 git** — 需要先有一份 canvas.html
3. **附件上传走 turn** — 上传 + send 看 agent 是否 Read 读到附件
4. **导出 PDF / Handoff** — 后端验证过；前端 button 链路待手动点

**测试步骤**（用户自己跑）：

```bash
# 后端已在 4001（用户的 npm run dev）
# 前端已在 5174（preview MCP 的 vite，或 cd web && npm run dev）

# 浏览器开 http://localhost:5174
# 1. 删掉 4 个 stale yolo 项目（点 ⋯ → 删除）
# 2. 新建项目 + 填 brief："做一个介绍 NoDesign 的 5 页 deck"
# 3. 看 chat 流 + iframe 出 HTML（~30s）
# 4. 双击文字改 → blur → 看 toast "已保存"
# 5. 刷新页面 → 改动还在
# 6. Export 顶栏按钮 → 选 HTML/PDF/Handoff 各下载一次验证
# 7. Inputs tab 拖一张图 → 进托盘 → chat "用这张图当封面" → send
```

## 已知风险点

1. **outerHTML 序列化丢内容**：C7 加了 `el.contentEditable='false'` 强制 commit DOM
   防御。上线后真测一遍输入中文 / 长文本 / 嵌套 contenteditable 边角。
2. **playwright PDF 启动延迟**：每次 spawn ~1-2s。P0 接受，P0+ 上 pool。
3. **WS 重连风暴**：Project.jsx 加 `hydrated && project` gate；ws-client 指数退避
   1s → 30s 上限；fatal close code（1008/1011）不重连。
4. **session 跨 turn cache hit**：SDK `query({ resume })` 是否真复用 prompt cache
   待真跑后看 tokendance 返的 cache_read_input_tokens 数据。
5. **stale yolo 项目**：DB 里几个 yolo 时建过的 project，workspace 目录不存在。
   Project 页能渲染（iframe 显 placeholder），但 PUT canvas / upload 会撞
   ENOENT。建议 e2e 测之前先在 Home 删掉它们。
6. **vite.config 改动需要重启 vite**：HMR 不重载 proxy 配置。本次 hotfix
   后用户需要 ctrl+c npm run dev 再起一次。下次再改 vite.config 也一样。
7. **tool_result 没带 name**：SDK 在 user message 里返回的 tool_result block
   不带 name 字段，loop.js handleUserBlocks 硬编码 '\<sdk-tool\>'。前端通过
   blockId 跟 tool_use 配对找到对应 name —— 不影响功能但 chat UI 显示稍粗糙。

## P0+ 延后清单（按优先级）

1. **D 流 inline comment 真接** —— 需先头脑风暴 anchor 序列化（DOM path / data-id / hybrid）
2. **agent screenshot 自检** —— Bash + playwright 调用 + Kimi vision 验证
3. **agent export 工具** —— Bash 调相同 export pipeline + SKILL.md 何时该交付
4. **agent 主动 fork variant** —— Bash `cp -r` + 前端 CanvasCandidateBar 真切换
5. **history revert UI** —— git log 列前端 + revert button
6. **F 流 custom slider** —— 头脑风暴 agent 怎么暴露 tweak schema
7. **H 流 DS 抽取** —— 完整工作流（含惯用法）深度
8. **PPTX 导出** —— pptxgenjs vs 自写转换器
9. **agent ask 工具** —— 当前通过 SKILL.md 引导文本提问；后续可加专用 ask 卡片

## 怎么找当时的具体细节

- 9 个 commit 自身 message 都很详细，可 `git show <hash>`
- `~/.claude/plans/dfh-commit-mellow-token.md` — 当晚 plan 文件，含 commit 切分依据
- `yolo-night-2026-04-29` 分支 — 4-29 晚的 8 个 commit 保留作参考（如果需要 cherry-pick）
- `~/.claude/projects/-Users-edy-Desktop-panel-workplace-Nodesign/memory/` — 持久记忆

## 心态备忘

跟 4-29 yolo 那晚（一晚 8 commit / 30+ 文件 / 用户失控感）对比：

- 本次每个 commit ≤ 6 文件，message 详细，syntax / build / smoke 都过
- 5 流（A/B/C/E/I）骨架完整 + e2e 验证 + 留给用户浏览器手动测真 LLM 跑
- 没碰 D/F/H —— 这些有未敲定的设计细节，强行做又是 yolo
- 速度 vs 节奏：每个 commit 后停下汇报 + 让用户决定推不推下一段

下一次扩展（D/F/H 任意一项 / agent 自检截图）继续守这个节奏。
