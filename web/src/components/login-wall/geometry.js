/**
 * 登录墙的尺寸约定（2026-08-17 从 AuthGate 拆出，供 wall-css 与 AuthGate 共用）。
 *
 * 整面墙是一张 1500x800 的设计稿，对着 1500x780 的**安全框**做 contain、顶边对齐：
 * 位置全用设计稿坐标，任何视口下构图都不会散。竖向富余一律留给底边（底排纸本来
 * 就设计成出血），顶边永不裁 —— 先用 cover 上过线，实机 1920x930 上下各切 47px
 * 被用户当场抓出「有点超画幅」。安全框比设计稿矮的这 20px 就是出血量。
 *
 * ⚠️ 新场景的纸要摆在 y=780 以下的话，自己先想清楚它被裁掉半截还成不成立。
 */
export const DESIGN_W = 1500;
export const DESIGN_H = 800;
export const SAFE_H = 780;

/** 顶栏高度（09-12 印刷风：顶栏不进设计稿、不缩放，稿从它下沿开始） */
export const NAV_H = 56;

/** 窄到这个宽度以下整面墙收起，只留登记卡 */
export const NARROW_W = 980;
