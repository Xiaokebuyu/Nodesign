---
name: stage-setup
version: 0.1.0
description: 开一个 RP 故事之前，把它拆成两种文件：角色卡（人，cast_role 写）和台面（世界 / 规矩 / 怎么演，open_stage 交），再用 open_stage 建好交出去；玩家在显示器的开场页挑写法预设、勾角色卡可选条目后自己点开始。用户说"我想玩个 RP""用这张卡演起来""换个设定重开"，或者他丢来一张酒馆角色卡/世界书，或者演到一半要改人设时用本 skill。含：卡装什么台面装什么、冻什么不冻什么的成本纪律、一人分饰多角的防串味写法、记忆怎么分家、open_stage 的参数怎么填。
---

# 这一步在干什么

演戏的不是你。你负责搭台：把一场戏拆成**两种文件**，交给演出进程，然后退到场务位。
台上的每一拍由它写，用户的话直接进它的队列，不经过你。

| 文件 | 装什么 | 谁写 |
|---|---|---|
| `角色/<名>/角色卡.md` | **一个人的全部**：他是谁、怎么说话、绝不做什么、语气样本；可有一节 `## 可选`（玩家开场时决定启不启用的设定）；加一块机器维护的"他记得的事"索引 | `cast_role`（用户在显示器的「角色」页也能改） |
| `台面.md` | **这场戏的全部**：世界、难度、代笔档、这个故事特有的文风铁律、怎么演 | `open_stage` 的 `table`（用户在显示器的「设定」页也能改） |
| 写法预设 | 通用的写作规矩（文风 / 叙事 / 人物 / 篇幅 / 思考清单），内置 Izumi 与文学派两套，酒馆预设 JSON 也能拆 | **玩家在开场页自己挑**，默认 Izumi；用户交了自己的预设时你把 JSON 放进 `<戏>/预设/`，或 `open_stage` 传 `style` |
| `规则.json` | 成就（奖杯）与剧情推进的阈值，机器只做比较 | `open_stage` 的 `achievements` / `triggers` |

**一场戏 = 工作区里的一个文件夹**（按标题起名），上面这些连同 记忆/、场景/、世界书/、预设/、素材/
都在里面 —— 一个文件夹就是一份能整个搬走的存档。画布上它是一张演出卡。项目 `CLAUDE.md` 不在里面，
那是你的档案，演出进程不读它，⛔ 别把台面规矩抄一份进去。

演出进程的系统提示词 = 台面 + 在场每个人的卡（原文，含玩家对「可选」一节的勾选）+ 这场戏的记忆索引 + 玩家挑的写法预设，别的什么都没有。
**人不进台面，世界不进卡**：八个人的戏写进八张卡就是八份世界副本，改一处漏七处；
反过来卡里只有人，这个人才能跟着卡跨项目搬。

改设定不用你重开：用户改了卡或台面，下一句话到时进程自己重开（他感知的只是那一句慢十秒）。
你改人设 = `cast_role` 重登同一个 id；你改规矩 = 再调一次 `open_stage`。

# 规则表：成就与推进

状态值由演出进程每拍在 `write_scene` 的 `state` 里报（必填，没变传空数组），开场值来自 `vitals` 的 `initial`，
机器另补一个 `拍数`。规则表拿这些值做比较，别的一概不做：

- **成就**（`achievements`）：`{id, title, desc, when, tier, hidden}`。达成弹奖杯、记进 成就.jsonl。
  阈值按用户选的难度定：爽档 40 就给"她笑了"，严酷档要 80。事件型的靠标志位：`牵手 == 1`
  （你在 vitals 里声明这个键，演出进程在戏里把它置 1）。隐藏成就达成前只显示"？？？"。
- **推进**（`triggers`）：`{id, when, note, once}`。条件一成立，机器把 `note` 当场务纸条接在演出进程
  这一拍的工具返回里，它下一拍照着推。从酒馆卡的分阶段规则翻：「好感过 60 进熟稔期」就是一条。
  `once` 默认 true；每次成立都要递的（放学了、天黑了）设 false。

`when` 的写法：`键 比较符 值`，用 `and` / `or` 连，比较符 `>= <= > < == !=`，不支持括号。
写错了 `open_stage` 当场退回。

# 角色卡怎么写

`cast_role` 一人一张。persona 里写：他是谁、怎么说话、绝不做什么，**带两三句语气样本** ——
样本比形容词管用十倍。「她说话短，不解释自己」不如直接给一句「"我听见了。"」。
别把世界观塞进卡；一句话就过去的路人（端酒的店小二）不写卡。
判据：**有名字、还会再出现、用户会想单独跟他说话的**，写卡。

**酒馆卡里"由玩家决定是否启用"的条目**（某条支线、某个癖好、某段前史、NSFW 倾向）写进卡的一节 `## 可选`，
一条一行：`- [ ] 有个弟弟 — 家里还有个初中的弟弟，周末要接他`（`[x]` 表示默认开）。显示器开场页把它们画成开关，
玩家勾完随卡进系统提示词（"启用 A，不启用 B"）。⛔ 别替玩家决定，也别把这类条目写死进正文。

卡的末尾有一块 `nd:memory` 标记块，是机器维护的记忆索引 —— 你和用户都别碰它，
正文在 `角色/<名>/记忆/` 里，演出进程用 `remember` 带 `who` 往里写。

# 台面怎么写

按这个顺序写，别加节：

```
## 世界
时代、地点、这个世界跟现实差在哪。三五行，不写设定集。

## 台面规矩
难度档、代笔档、有没有骰子、**这个故事特有的**文风铁律（比如"意象是暗线只写行为"）。
通用的写作规矩（禁转折句、对白不挂语气、篇幅、人称…）不用写 —— 那是写法预设的事，玩家开场时自己挑。
用户在开场问答里选的东西全落这儿。

## 怎么演
每一拍走 write_scene；不可逆的变化用 remember 记一条（某个人记得的事带 who）；掷骰用 roll。
一拍别超过一屏。这场戏特有的口味写这儿：骰子用 d20 还是 d100、多久存一次档、
把手里那枚"意想不到"偏哪个方向。
```

# 冻什么，不冻什么

系统提示词是**冻结区**：它每一轮原样重发，命中缓存就几乎不要钱，一改就整份重付。

- ✅ 冻：世界、人物、规矩、文风。这些整场不变。
- ⛔ 别冻：当前时间地点、状态数值、这一拍轮到谁、刚触发的世界书条目。
  这些每轮都在变，写进系统层等于**每轮把整份提示词重付一遍**。

真账：酒馆四方世界卡实测每回合输入 75k-95k token 而**缓存读恒为 0**，一回合约 $1。
根因就是世界书按 order 插进上下文中部，让后面的内容整体位移，前缀永不复用。
我们这边同一场戏第二拍起整块命中（实测读回 12 万）。差别不在模型，在这一条纪律。

**⛔ 不要把项目 CLAUDE.md 塞进去。** 那是设计工作台的东西（构建命令、部署规矩、
代码约定），进了戏就是污染。演出要的设定全部写在这份提示词里。

# 一人分饰多角

台上所有人由一个会话写。这是定案，不派子代理。

最大的风险是**所有人一个腔**。防它不靠小心，靠两件事：

1. **每个反复出场的人在"人物"节里有自己的段落**，带语气样本。样本要写他真会说的话，
   不是描述他怎么说话。「她说话短，不解释自己」不如直接给一句「"我听见了。"」。
2. **每个人都有自己的卡**（`角色/<名>/角色卡.md`），开戏时整份进提示词，提示词里还留一句
   "开口前拿不准腔调就 Read 一遍他的卡"。演出进程有 Read，用户也能在画布上改那个文件。

判据：**有名字、还会再出现、用户会想单独跟他说话的**，写卡；一句话的过场自己带过。

# 上一场记住的事怎么接回来

**不用你贴。** 记忆有两个家，`open_stage` 交出去时机器都接上：
- **某个人记得的事**（态度、秘密、跟谁的关系）在他的卡上（`角色/<名>/记忆/` + 卡末尾的索引块），
  随卡整份进提示词 —— 这个人换一场戏、换一个项目还是他。
- **这场戏的事**（演到哪、伏笔、世界新事实）在 `stage/memory/`，索引接在提示词末尾。

都只贴索引不贴正文：索引一条一行、常驻很便宜；正文按需 Read，演出进程自己判断哪条用得上。
⛔ 别自己去读那些目录再摘要进台面 —— 那是上一场的演出进程留给自己的话，你摘一遍等于两份真相。

# 三件工具怎么教

演出进程手上只有这三件（外加 Read/Glob/Grep/Skill）。**基本用法机器会自动接在提示词
末尾**（「台面」一节：每拍 write_scene 带把手、不可逆的事 remember、掷骰 roll、不用
ToolSearch 找别的）。你在"怎么演"里只写这场戏特有的：多久存一次档、骰子用 d20 还是
d100、把手里那枚"意想不到"的口味。下面是三件工具各自的要点，写提示词时心里有数就行：

- **`write_scene`** —— 台上只认它写进去的东西。**正文之外不要再复述一遍剧情**，
  那会在画布和聊天里各出现一次。每次必须带 2-4 枚把手：一枚推进主线、一枚人际、
  一枚合理但意想不到的。**没有把手这一拍就没写完**。
  （真会话数据：342 条板书里只有 120 条带把手，35%。这是漏得最多的一条。）
- **`remember` / `forget`** —— 一事一份，形状照搬 harness 那套 auto-memory：
  frontmatter 里 `name` / `type` / `description`，正文写事实不写散文，
  写完索引自己重建。四类：`progress` 演到哪了、`character` 某个人的态度与他记得的事
  （**要带 `who`**，写进他的卡）、`thread` 伏笔、`world` 演出中确立的设定。
  **只在不可逆的事情发生时记**：关系变了、伏笔埋下或收回、世界里确立了新事实、
  玩家做了回不了头的选择。⛔ 别记这一拍的流水账，正文本身就在台上。
  同一件事再记就用同一个 `name` 覆盖，别新建第二份；剧情推翻了旧设定用 `forget` 删掉，
  别留两份打架的。
- **`roll`** —— 服务端真随机。⛔ 永远别自己编点数，编的会塌掉整桌的信任。


# 交出去：open_stage

卡都写好、开场三问问完，调 `open_stage` 一次：

| 参数 | 填什么 |
|---|---|
| `title` | 这场戏的名字，卡上和显示器顶栏都显示它 |
| `table` | 台面全文（上面那三节），写进 `stage/台面.md` |
| `cast` | 在场者 `[{name, note?}]`。**每个 name 都要已经有卡**，没卡它会拒开。一人 = 画立绘，多人 = 画名册；小字不给就用卡上的 note |
| `vitals` | 状态面板 `[{key, label?, as, max?, unit?, options?, initial?, who?}]`。`as`：bar 进度条 / chips 几格亮一格 / num 数字 / text 一行字；`who` 是这个值属于哪个在场者（好感度属于她；时间 / 天气不填）。演出进程在 write_scene 的 `state` 里按 key 更新 —— ⛔ **key 要跟你在台面、规则里写的一字不差**（09-05 一场 vitals 用英文键、进程报中文键，面板永远是 0）。不需要面板就不传 |
| `skin` | **留默认 paper**（跟平台一致）。别替用户挑外观，他在显示器的「状态」页自己换 |
| `style` | 写法预设 `{preset, modules?}`。**通常不传** —— 玩家在开场页自己挑（默认 Izumi）。只在用户交了自己的酒馆预设 JSON 时：把文件放进 `<戏>/预设/<名>.json`，传 `preset: "user:<名>"`；机器会把条目拆成开关 |
| `achievements` / `triggers` | 规则表，见上一节。没设也能演，只是没有奖杯和推进纸条 |

调完你就是**场务**：画布上多了一张卡，用户双击进去先到**开场页**（看世界与人物、挑写法、勾可选条目、点开始），
机器自己写开场指令、生开场背景图；进程到那时才起。之后他说的每句话直接进演出进程，不经过你。
⛔ 别自己替他"说第一句"去开场 —— 开场是机器和玩家的事。⛔ 之后别在对话里代演、别复述台上的剧情。他回到对话里跟你说话，
才是在跟你说话（改设定 / 换玩法 / 问怎么用）。

用户要"从头再来"让他自己删画布上那张卡（那场戏的文件夹），别替他删。

# 开场先问清楚三件事

用 `AskUserQuestion` 一次问完，答案直接落进"台面规矩"节：

- **想跟谁演** —— 一对一 / 一群人 / 由你安排
- **难度** —— 爽（世界顺着他）/ 写实（他和普通人同权）/ 严酷（世界带恶意，
  但漂亮的输入该赢还是赢）
- **代笔到哪一步** —— 全代入（他的角色你一个字不代写）/ 大纲档（他给大意你扩写，
  但重要抉择必须停笔等他）

顺带问一句**有没有自己的写法预设**（酒馆预设 JSON）：有就让他丢进对话或工作区，你放到 `<戏>/预设/` 下；
没有就不用管，开场页默认给 Izumi 那套，他自己勾。

用户丢来酒馆卡或世界书时，先加载 `story-import`，落点表在那儿。
剧情推不动时加载 `story-craft`。（`story-voice` 的具名文风现在是写法预设的一部分，玩家在开场页挑。）

# 写完自查

- [ ] 台面三节齐了，里面没有人物（人物在卡上）
- [ ] 每张卡都有语气样本，不是只有形容词
- [ ] 没有任何每轮会变的东西（时间、数值、当前场景）
- [ ] 没有项目 CLAUDE.md 的内容
- [ ] cast 里每个名字都有卡，且跟卡 frontmatter 的 name 一字不差
- [ ] 规则表的键都在 vitals 里声明过（或是机器补的 拍数），阈值按难度定过；vitals 的 key 跟台面 / 规则里的写法一字不差
- [ ] 酒馆卡里"玩家自选"的条目进了卡的 `## 可选` 一节，没有替他决定
- [ ] skin 留的默认，没替用户挑外观
- [ ] 调了 open_stage，而不是自己在对话里开演
